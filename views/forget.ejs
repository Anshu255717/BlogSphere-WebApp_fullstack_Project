<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogSphere - Forget Password</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="images/fav.jpeg">
    <style>
        body {
            background-color: #f8f9fa; 
            font-family: Arial, sans-serif;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: stretch; 
            height: 100vh;
            padding: 20px;
        }
        .form-container {
            flex: 1;
            max-width: 500px;
            padding: 20px;
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .image-container {
            flex: 1;
            background: url('/images/forget.gif') no-repeat center center;
            background-size: cover;
            transition: transform 0.5s ease-in-out; 
            height: 100%; 
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .form-container:hover ~ .image-container {
            transform: scale(1.05); 
        }
        .btn-custom {
            background-color: #8996d3; 
            color: #ffffff;
            border-radius: 5px;
            font-size: 16px;
            padding: 10px 20px;
        }
        .btn-custom:hover {
            background-color: #7398de; 
        }
        .back-link {
            display: block;
            margin-top: 20px;
            text-align: center;
            color: #5b6a8e;
            text-decoration: none;
        }
        .back-link:hover {
            text-decoration: underline;
        }
        .message {
            color: #dc3545; 
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-container">
            <h3 class="text-center mb-4">Forgot Your Password?</h3>
            <% if (typeof(message) !== 'undefined') { %>
                <p class="message" id="error-message"><%= message %></p>
            <% } %>
            <form action="/admin/forgot-password" method="POST">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
                </div>
                <button type="submit" class="btn btn-custom btn-block">Send Password Reset Link</button>
            </form>
            <a href="/admin/login" class="back-link">Back to Login</a>
        </div>
        <div class="image-container"></div>
    </div>
    
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
       
        $(document).ready(function() {
            const errorMessage = $('#error-message');
            if (errorMessage.length) {
                setTimeout(function() {
                    errorMessage.fadeOut('slow', function() {
                        $(this).remove(); 
                    });
                }, 1000); 
            }
        });
    </script>
</body>
</html>
